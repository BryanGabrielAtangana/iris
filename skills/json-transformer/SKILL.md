---
name: json-transformer
description: Transform, filter, and reshape JSON documents.
when_to_use: Use when transforming, filtering, or reshaping JSON data structures.
examples: ["reshape this json", "filter json objects by a field"]
tags: [json, data, transform, reshape]
version: 1.0.0
requires: { code_execution: true }
---
# json-transformer
Parse the JSON, apply the mapping/filter/restructure, and emit valid JSON.
