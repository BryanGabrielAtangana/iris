---
name: http-request
description: Make HTTP requests to real APIs and inspect the responses.
when_to_use: Use when calling a real HTTP API endpoint and inspecting its response.
examples: ["call this REST endpoint and show the response", "send a POST request with a json body"]
tags: [http, api, client, request]
version: 1.0.0
requires: { code_execution: true }
---
# http-request
Build the request (method, headers, body), send it, and inspect status and payload.
