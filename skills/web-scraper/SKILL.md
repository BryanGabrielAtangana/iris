---
name: web-scraper
description: Extract structured data from web pages and HTML.
when_to_use: Use when scraping or pulling structured data out of websites or HTML.
examples: ["scrape product prices from a site", "extract all links from a page"]
tags: [web, scraping, html, extraction]
version: 1.0.0
requires: { code_execution: true }
---
# web-scraper
Fetch the page, parse the DOM, select the target nodes, and emit structured rows.
