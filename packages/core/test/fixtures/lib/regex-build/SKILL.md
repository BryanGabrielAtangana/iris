---
name: regex-build
description: Build and test regular expressions for matching text.
when_to_use: Use when constructing or debugging regular expressions.
examples:
  - write a regex to match emails
  - build a pattern for phone numbers
tags: [regex, text, matching]
version: 1.0.0
requires:
  code_execution: false
---

# regex-build

Build and test regular expressions for matching text.

## Steps

1. Identify the inputs.
2. Run the relevant tooling.
3. Return the result to the user.
