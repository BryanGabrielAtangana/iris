---
name: docker-compose
description: Author and run multi-container Docker Compose stacks.
when_to_use: Use when defining or running Docker Compose services.
examples:
  - set up a postgres and redis stack
  - write a docker-compose file
tags: [docker, devops, containers]
version: 1.0.0
requires:
  code_execution: true
---

# docker-compose

Author and run multi-container Docker Compose stacks.

## Steps

1. Identify the inputs.
2. Run the relevant tooling.
3. Return the result to the user.
