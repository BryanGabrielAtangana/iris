---
name: sql-migrate
description: Author and run relational database schema migrations.
when_to_use: Use when creating or applying SQL schema migrations.
examples:
  - add a column to the users table
  - write a database migration
tags: [sql, database, migration]
version: 1.0.0
requires:
  code_execution: true
---

# sql-migrate

Author and run relational database schema migrations.

## Steps

1. Identify the inputs.
2. Run the relevant tooling.
3. Return the result to the user.
