{
  "name": "iris",
  "version": "0.1.0",
  "description": "Iris CLI — manage a portable skill library and sync it into your agents.",
  "license": "Apache-2.0",
  "type": "module",
  "bin": {
    "iris": "./dist/index.js"
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsup",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "lint": "eslint src",
    "clean": "rm -rf dist .turbo"
  },
  "dependencies": {
    "@iris/adapters": "workspace:*",
    "@iris/core": "workspace:*",
    "@iris/protocol": "workspace:*",
    "commander": "^15.0.0"
  }
}
