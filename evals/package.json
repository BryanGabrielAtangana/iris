{
  "name": "@iris-sylvia/evals",
  "version": "0.0.1",
  "private": true,
  "description": "Iris discovery-accuracy benchmark: Iris two-tier retrieval vs a naive flat-description baseline.",
  "license": "Apache-2.0",
  "type": "module",
  "scripts": {
    "build": "tsup",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "lint": "eslint src",
    "bench": "tsx src/run.ts",
    "clean": "rm -rf dist .turbo",
    "accuracy": "tsx src/accuracy.ts",
    "benchoff": "tsx src/bench.ts",
    "abstain": "tsx src/abstain.ts",
    "sweep": "tsx src/sweep.ts",
    "discovery": "tsx src/discovery.ts"
  },
  "dependencies": {
    "@iris-sylvia/core": "workspace:*",
    "@iris-sylvia/embeddings": "workspace:*"
  }
}
